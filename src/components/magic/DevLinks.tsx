import classNames from 'classnames'
import Link from 'next/link'

const DevLinks = ({
	primary = false,
	footer = false,
}: {
	primary?: boolean
	footer?: boolean
}) => (
<>
</>

)

export default DevLinks
